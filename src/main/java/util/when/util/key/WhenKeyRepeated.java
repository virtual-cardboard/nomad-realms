package util.when.util.key;

import context.input.event.KeyRepeatedInputEvent;

public class WhenKeyRepeated extends AbstractWhenKey<KeyRepeatedInputEvent> {

	public WhenKeyRepeated(int key) {
		super(key);
	}

}
